# Behavior-preserving maintenance review — September 10, 2026

## Scope and source authority

This closes the approved maintenance work, not Precision Lab release acceptance. No product feature, layout rewrite, scientific calculation, evaluation cadence, storage format, worker protocol, runtime toolchain upgrade, dependency update, or bundle-limit change is included.

- Intake: `6de861dd4e00f3223ec8985a0b6c00bdbfa7abe9`, tree `12a57ef8e463e7386c687b602f2501de088d798c`.
- Branch: `codex/nn-forge-precision-lab`.
- Main remains `98f29b86e469a2a545be75928ae6f32309fd1582`.
- Maintenance code initially qualified: `5092ee69abc8714ff906d5d63bbd5b5263f62368`, tree `02ae546bb00132affd98926045890fdf59331ab7`, Actions run `34529946854`.
- Final reporting-only review correction: `7ec856a39079141a040624e9fe2927218e0db8f8`, tree `4447aed98747a411e9fef2211cb2785de1c2bde6`. This adds exact tree comparison to completion receipts; it does not change application output.
- The documentation closure is a descendant of those commits. Its exact-head Actions results must be inspected separately; this record does not invent its own commit SHA or future CI result.

The source archive was recovered through GitHub Actions, checked against its archive digest and exact Git tree, then edited in an isolated linked worktree. GitHub publication used Git data trees and coherent commits with a non-forced branch update. Synthetic local recovery history was not pushed.

## Completed work and commit evidence

| Task | Result | Implementation evidence |
|---|---|---|
| M1: production lint | Explicit `any` is an error in production; malformed test/benchmark inputs have narrowly scoped exceptions. Runtime fault adapters are not exempt. A structural optional adapter-limit field removes the one production WebGPU `as any` without changing its fallback. | `ee615300`; nine real ESLint policy tests. |
| M2: architecture | Local ESLint rule prevents reverse engine/shared dependencies, UI imports in domain packages, test/prototype imports in production, and new controller-owner modules. Exact legacy adapters remain documented exceptions. | `a2a68762`; 24 real ESLint positive/negative cases. |
| M3: failure evidence | Command receipts preserve exits, signals, source identity and raw logs. Small validated summaries retain failure/skipped/flaky/incomplete distinctions. Playwright JSON accompanies HTML; build manifests survive a later bundle failure; both recovery reports are preserved. | `d212779a`, tightened by `7ec856a3`; nine wrapper tests, 17 summary tests and workflow contracts. |
| M4: test fixtures | Two duplicated saved-run builders become one typed test-only factory with deterministic metadata and isolated nested objects. Existing History and save assertions are retained. | `acf04179`; three factory tests plus the affected suites. |
| M5: unused code | Delete only private `Row`, `Segmented` and `Stepper` primitives with no tracked import/JSX/file consumers. Make four private test-projection interfaces non-exported. No CSS or legacy-shell removal. | `5092ee69`; tracked reference/reachability review, full tests/types, emitted-output comparison. |
| M6: pure decisions | Characterize save-disabled reason priority and access-only/hydration-only invalidation. Retain the already-local pure guard instead of introducing another abstraction or moving App ownership. | `acf04179`; priority/transition tests and subscription mutation probe. |
| M7: resource ownership | Review targeted listeners, observers, RAF/timer and worker lifecycles. Add repeated StrictMode subscription-cycle and late-save-completion regressions. No speculative production lifecycle changes. | `acf04179`; focused lifecycle tests and cleanup mutation probe. |
| M8: contributor/verification map | Document environments, ownership, evidence commands, connector recovery and retained/deleted candidates. Reconcile stale current-state/bundle/performance text in the living plan without checking off the blocked release. | This documentation closure; source and test receipts listed below. |

The step-by-step plan is `docs/maintenance/2026-09-10-plan.md`, committed before implementation in `38179180`.

## Tests that detect the intended mistakes

The new lint policy first rejected four production-policy expectations under the old global exemption; the configured policy then passed. Forbidden architecture cases similarly failed before the rule existed. The command wrapper and summary tests were exercised before implementation and after fixes.

Three deliberate local mutations were killed and then restored: removing the fixture recipe clone, removing the storage-listener unsubscribe, and removing the save controller's access subscription. Each relevant regression exited nonzero. These checks are not a claim of repository-wide mutation coverage.

Review also found and corrected reporting edge cases. Parent cancellation must remain interrupted even if the child exits gracefully with code zero. A fabricated expected/actual Playwright status, changed tracked source after a command, and a syntactically valid but wrong source tree must not produce passing evidence. Their failing regressions were observed before the corrections. The installed Playwright reporter was exercised with browser-free passing, failing, skipped and expected-failure tests; the summary retained their distinct outcomes.

## Retain/delete inventory

The import inventory used TypeScript syntax and tracked entry points: application, worker, lazy modules, package exports, tests, scripts and prototype roots. It was combined with whole-tree symbol, filename and JSX searches. This is a scoped deletion review, not a universal proof that every remaining declaration is live.

`Row.tsx`, `Segmented.tsx` and `Stepper.tsx` had only their own export declarations and no tracked consumers; their deletion leaves local emitted artifacts unchanged. `LegacyDataProjectionForTest`, `LegacyFeaturesProjectionForTest`, `LegacyTrainingProjectionForTest` and `LegacyTrainingSnapshotForTest` are used internally in one test helper, so their interfaces remain but unnecessary exports are removed.

Retained deliberately: `RegionShell`, legacy/default `MainArea`, `Sidebar`, compatibility layout aliases, public package exports, test setup/declarations, and every stylesheet. Their compatibility or non-import consumers require a separately qualified migration. `NetworkGraphSVG` is a live fallback, not unused code. No dependency was removed on the basis of a simple text search.

## Resource ownership review

| Resource | Owner and release contract reviewed | Evidence/boundary |
|---|---|---|
| Saved-memory `storage` listener | `useExperimentMemoryStorageSync` subscribes in the effect and removes on cleanup; storage denial installs no listener. | Existing SecurityError tests; new three-cycle StrictMode test; unsubscribe mutation killed. |
| Accepted asynchronous save | `useSaveCurrentRun` owns the synchronous busy guard and mounted display flag; an accepted artifact may finish persistence after unmount. | Late completion stores the original artifact once; competing capture remains blocked. No persistence concurrency change. |
| Save display invalidation | Hydration/access subscriptions refresh the displayed disabled reason while commands synchronously read current stores. | Access-only and hydration-only tests; removal of access subscription is caught. The apparently unused subscriptions remain. |
| Timed state | `useTimedState` clears replacement/unmount timers and handles retained setters after unmount. | Existing focused tests reviewed and full suite run; no production changes. |
| Modal containment | `useModalFocusContainment` releases listeners/observer/leases and restores prior attributes/focus. | Existing focused and StrictMode tests reviewed and run; no global focus rewrite. |
| Training/worker snapshots | `useTraining` unsubscribes snapshots, terminates the worker on cleanup and uses epoch invalidation for obsolete operations. | Existing integration suite; ownership unchanged. |
| Chart/graph presentation | LossChart/graph resize observers and animation frames have explicit cleanup; graph wheel listeners and worker-bridge RAF are released by their owners. | Source review plus existing suite. No browser heap profiling or claim that all possible leaks are eliminated. |

The cross-tab saved-run lost-update finding remains open in `BUGS-TO-REVIEW.md`. Exact-artifact retries do not solve that separate issue.

## Verification receipts

Local final checks: 154 Node helper tests, 497 engine tests, 334 shared tests and 1,188 web tests pass. Package total is 2,019; the increase from intake is nine test cases, not nine product features. Lint, full source/test typechecks, build and whitespace review pass. The final helper count includes the reporting-only tree-consistency correction after the initial CI code commit.

Actions `34529946854` on `5092ee69` independently passed changed tests, the helper suite at that revision (153 tests), the complete 2,019-test package suite, lint, typechecks, production build, source capture, fault-enabled recovery, clean normal rebuild, both fault-disabled tests, and the unchanged accepted five-pair performance comparison. The bundle guard remains failed, and its new compact summary accurately records the failure rather than converting it into a pass. Build provenance is nevertheless retained in artifact `10173293431`.

Browser receipts from the same maintenance run remain failures, not reporting errors. Preview reports 69 expected, 5 unexpected, 0 flaky and 8 skipped/not-run results; subpath reports 50 expected, five unexpected, zero flaky and 27 skipped/not-run results. Both stopped at the existing five-failure cap. The summaries expose the current accessibility/zoom failures and, in subpath, a WebKit evidence-convergence timeout. Skipped/not-run counts after early termination are not all intentional skips. No browser fix, tolerance adjustment or retry was introduced here.

Measured bundle values:

| Environment | Entry gzip | Inspection gzip | Total JavaScript gzip | Verdict |
|---|---:|---:|---:|---|
| Local intake and maintenance | 151341 / 152245 | 5431 / 7373 | 234285 / 234161 | Existing total overrun: 124 bytes. |
| Fresh CI maintenance `5092ee69` | 151342 / 152245 | 5431 / 7373 | 234282 / 234161 | Total overrun: 121 bytes; gate correctly fails. |

All 13 locally emitted output files, including JavaScript, worker, CSS and HTML, are byte-identical before/after maintenance. Recovered local dependencies omit font binaries, so that comparison is explicitly limited to locally emitted files, not complete shipping font assets. Fresh CI installs the actual dependencies/assets. No runtime speed improvement or smaller shipped bundle is claimed from this maintenance work.

Compact summaries are independent from full logs/traces. Initial real CI recovery summary ZIP is 582 bytes, focused summary ZIP 1078 bytes and performance summary ZIP 887 bytes. Each contains exact source/attempt metadata. Raw command evidence and original browser/performance artifacts remain available. These reductions concern evidence retrieval size, not application performance, and do not repair a connector outage.

## Acceptance boundary

Maintenance has its own scoped tests and output-equivalence evidence. It does not transform the already-failing Precision Lab branch into a qualified release. Existing browser/zoom acceptance, the total-JavaScript overrun, consumer-proven legacy-shell retirement, and cross-tab persistence remain separate work. No threshold, browser assertion, test retry setting, scientific update cadence, main branch, repository visibility, Pages setting or deployment was changed.
