# Behavior-preserving maintenance implementation plan

> Execution: task-by-task with test-first checks and GitHub Git-data commits. The user's September 10 approval covers this maintenance pass; it does not authorize features, a merge, deployment, or repairing the separate Precision Lab acceptance failures.

**Goal:** improve production lint policy, enforce existing ownership boundaries, preserve small CI failure receipts, consolidate test fixtures, and document evidence-backed cleanup decisions.

**Architecture:** App remains the production controller owner. Maintenance tools run in Node/CI, not in the application. Test fixtures stay under `apps/web/src/test`; existing browser assertions and performance policy remain untouched.

**Tech stack:** existing ESLint 9/typescript-eslint, Node, Vitest, Playwright JSON reporter, pnpm 9, GitHub Actions. No dependencies added or upgraded.

**Specification:** the approved eight-item maintenance discussion, constrained by `docs/superpowers/specs/2026-07-16-precision-lab-production-integration-design.md` and its July 17 plan.

## Source and invariants

- Intake branch: `codex/nn-forge-precision-lab` at `6de861dd4e00f3223ec8985a0b6c00bdbfa7abe9`, tree `12a57ef8e463e7386c687b602f2501de088d798c`.
- Main is `98f29b86e469a2a545be75928ae6f32309fd1582`; leave it unchanged.
- Recover the exact GitHub source artifact and verify its tree before editing. Recovered dependencies may be used only with the matching lockfile.
- Do not add product features, alter scientific cadence/math, storage format/concurrency, worker/URL contracts, controller ownership, CSS layout, or browser assertions.
- Bundle caps remain 152245/7373/234161 gzip bytes. Existing failures remain failures; maintenance acceptance is not release acceptance.
- No repository visibility, Pages, secrets, settings, deployment, forced ref, or merge changes.
- Publish coherent commit trees through the connector; re-read the branch before non-forced fast-forward publication. On concurrent movement, reconcile rather than overwrite.
- Check boxes only with test/review/commit evidence. This document records scope; final receipts belong in the living execution plan and maintenance record.

## M1 — Production lint scoping

**Files:** `eslint.config.js`, `scripts/lint-policy.test.mjs`, `packages/engine/src/webgpu/types.ts`, `packages/engine/src/webgpu/detect.ts`.

1. Record baseline tests/build/bundle; do not misclassify pre-existing failures.
2. Add ESLint API tests that require `@typescript-eslint/no-explicit-any` on app/engine/shared production paths and allow intentional malformed fixtures on explicit test paths. Example input: `export const value: any = 1;`.
3. Run `node --test scripts/lint-policy.test.mjs`; observe production-policy assertions fail before changing configuration.
4. Remove the global `any` exemption; add test-only overrides. Add the known optional adapter limit to the structural WebGPU type and replace its single production `as any` with typed property access. Preserve the exact fallback/expression.
5. Run policy tests, `pnpm lint`, and `pnpm typecheck`. Do not add blanket suppressions or cast through `unknown` to evade the new rule.

## M2 — Enforce existing dependency ownership

**Files:** `scripts/eslint-architecture.mjs`, `scripts/architecture-policy.test.mjs`, `eslint.config.js`.

1. Inventory existing controller imports/calls and package boundaries before setting rules.
2. Test forbidden app imports in engine/shared, production imports of fixtures/prototypes, and new controller owners; also test allowed domain dependencies, type-only imports, and explicit legacy wrappers.
3. Cover re-exports, literal dynamic imports, and relative path normalization, not only happy-path static imports. Negative example: importing `useTraining` into a new presentation component.
4. Implement one local ESLint rule using resolved repository-relative paths. Preserve exact existing legacy-wrapper exceptions with reasons rather than deleting unqualified legacy consumers.
5. Run rule tests through real ESLint configuration and `pnpm lint`. Document that import guards prevent new owner modules, not arbitrary runtime instance counts.

## M3 — Compact, honest CI failure evidence

**Files:** `scripts/run-with-evidence.mjs`, `scripts/run-with-evidence.test.mjs`, `scripts/summarize-qualification.mjs`, `scripts/summarize-qualification.test.mjs`, `.github/workflows/precision-lab-verification.yml`, `playwright.config.ts`, `.gitignore`, `scripts/precision-lab-workflow.test.mjs`.

1. Test a subprocess receipt for exit 0, exit 7, missing executable, streamed output, exact arguments, and unsafe receipt names; no shell interpolation or environment dumping.
2. Implement a streaming command wrapper that writes raw logs plus command/exit/signal/source receipts and preserves the child's failure status.
3. Test report summaries for passing, failing, skipped, flaky, interrupted, malformed, missing, and inconsistent JSON; bound error excerpts and failure lists. Missing reports must never become passing evidence.
4. Add Playwright JSON beside existing list/HTML reports; preserve recovery JSON before the normal rebuild.
5. Wrap existing qualification commands without changing their assertions, timeouts, retries, max-failures, benchmark sampling, or caps. Capture bundle measurements even when its guard fails. Publish small summaries with `always()` separately from full raw evidence.
6. Test the YAML's reporting contracts and exercise summarization against real failed CI artifacts when available. Keep reporting errors distinguishable from application failures.

## M4 — Typed fixture consolidation and isolation

**Files:** `apps/web/src/test/savedRunFixtures.ts`, `apps/web/src/test/savedRunFixtures.test.ts`, `apps/web/src/hooks/useSaveCurrentRun.test.tsx`, `apps/web/src/components/controls/RunHistoryPanel.test.tsx`.

1. Compare duplicate record builders byte-for-byte; preserve independent assertions and all existing cases.
2. Add tests for deterministic IDs/timestamps/evidence defaults and isolated nested objects between factory calls.
3. Extract the duplicated typed builder to the test-only directory, preserving its caller arguments/defaults. Test expected values independently of the production serializer.
4. Replace duplicate builders in both suites and add explicit cleanup for any newly introduced mocks/globals.
5. Run both affected suites, the factory tests, typechecks, and the complete package suite. Prove a fixture mutation is caught rather than claiming quality from shorter files alone.

## M5 — Evidence-backed unused-code inventory

**Files:** `docs/maintenance/2026-09-10-review.md`; only individually proven unused declarations may be removed.

1. Inspect import/re-export reachability from app entry, worker entry, lazy modules, package entry points, tests, scripts, and prototypes.
2. Search candidate symbols in the entire tracked tree, including docs and non-code consumers.
3. Record retain/remove decisions and reasons. Public package exports, legacy wrappers under acceptance, dynamic references, and CSS are not deletion candidates based solely on zero textual imports.
4. Remove only demonstrated private unused declarations; when none meets that bar, retain them and record a completed audit with no speculative deletion.
5. Run lint/typecheck/tests and review the deletion diff, if any.

## M6 — Small pure decisions, not ownership churn

**Files:** scoped candidate review in `docs/maintenance/2026-09-10-review.md`; characterize `apps/web/src/hooks/useSaveCurrentRun.test.tsx`.

1. Inspect App's existing pure selectors/controllers and the synchronous save guard.
2. Add independent priority/transition assertions for busy, pending, loading, incompatible, and ready cases where coverage is absent.
3. Only extract a decision if it removes meaningful duplication without new subscriptions, changed lifecycle, changed copy, or additional bundle pressure. Otherwise record why retaining the existing helper is safer.
4. Keep App's four production controller instances and live subscriptions intact. In particular, do not remove hydration/access subscriptions whose values are read synchronously by command guards.
5. Run focused characterization tests and the full suite; no arbitrary hook splitting or blanket memoization.

## M7 — Resource-ownership audit

**Files:** `docs/maintenance/2026-09-10-review.md`, `apps/web/src/hooks/useExperimentMemoryStorageSync.test.tsx`, scoped lifecycle tests as supported by evidence.

1. Inventory listeners, timers, observers, RAF handles, worker ownership, and store subscriptions in affected lifecycle paths.
2. Confirm acquire/release behavior and StrictMode replay using existing tests; add a mount/unmount/remount lifecycle regression where missing.
3. Test late completion of a pending save after component unmount without replacing its artifact or permitting a duplicate capture.
4. Perform a temporary mutation of a cleanup/guard locally, prove the relevant regression fails, and restore it before committing.
5. Do not fix unproven races or the separately deferred cross-tab overwrite. Record audit coverage limits explicitly.

## M8 — Reproducibility, contributor map, and final receipts

**Files:** `docs/maintenance/README.md`, `docs/maintenance/2026-09-10-review.md`, `NN-FORGE-LIVING-EXECUTION-PLAN.md`.

1. Document supported Node/pnpm ranges separately from the exact observed CI versions; no runtime upgrade disguised as cleanup.
2. Document controller/store/buffer ownership, test-only paths, reporting commands, artifact naming, and recovery steps through the connector.
3. Reconcile stale current-status prose in the living plan while preserving historical receipts and unresolved acceptance boxes.
4. Review changes against all eight tasks and the protected-path list; test helpers, lint, typechecks, full tests, build, and unchanged bundle guard.
5. Create small GitHub commits, publish with a non-forced ref update, verify the remote tree, and inspect exact-head CI. Report inherited failures explicitly, not as successful product acceptance.
