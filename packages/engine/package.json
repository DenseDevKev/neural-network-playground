{
    "name": "@nn-playground/engine",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "test:perf": "vitest run --config vitest.perf.config.ts",
        "bench": "pnpm run test:perf"
    },
    "devDependencies": {
        "vitest": "^3.0.0",
        "typescript": "^5.7.0"
    }
}
