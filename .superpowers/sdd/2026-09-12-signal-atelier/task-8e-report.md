# Task 8E — visual fidelity and usability completion

Implemented the scoped UI wave on the isolated Signal Atelier checkout, preserving the parent's uncommitted owned-file baseline. No worker, scientific controller, draft authority, persistence controller, existing browser-test, or build changes were made by this task. Parent explicitly expanded ownership to display-only changes in GuidedLessonPanel and BoundaryEvidencePanel.

## Changes

- The single training toolbar now sits in normal flow above every destination's content. Mobile exposes Play/Pause, Step, Reset, finite speed selection, and Checkpoints immediately with 44px targets; step, epoch, and lifecycle status share a compact secondary line. No training behavior changes or navigation-induced pauses. Parent was notified to update old sticky-position browser assertions.
- Mobile heading/header/tab spacing is reduced. The optional first-visit invitation appears only on Network; Lessons remains reachable everywhere. Mobile Share setup and Save run shortcuts are available through Export/import utilities and Saved runs navigation rather than duplicated heading buttons. Prediction now includes the real full-evaluation metrics and age/provenance, in addition to the sampled prediction-grid provenance.
- Setup uses a bounded, independently scrolling form and a separate normal-flow Apply/Cancel footer. This replaces an intermediate sticky-footer approach that actual screenshots showed would still cover fields. Native inputs, all nine features, all six layer slots and 1–16 neuron limits remain available. Feature chips have 44px labelled native-checkbox targets, explicit selected states, and keyboard focus. Draft architecture shows selected feature labels, numbered square neuron units, and bounded decrement/increment controls. These are topology units, not fabricated activation maps.
- Saved primary actions use the canonical action tokens with a corrected cascade; generic button exclusions use `:where` so they do not unexpectedly outrank header styles. Save current run computed colors are white on #C74424 in Light and #17191B on #EF653F in Dark, 46px actual button height. Duplicate Saved and Lessons visible headings and the long saved-apply protocol paragraph were removed; concise trained-parameter/reset consequences remain beside interaction and the full consequence remains in confirmation.
- Comparison has 18 aligned primary rows for dataset, split/seed/noise, evaluated populations, architecture/features, activation/initialization/model seed, optimizer/rate/batch/schedule/loss/regularization/clipping, evaluated step and loss/objective/accuracy. Display numbers are readable; exact numbers remain in cell titles and complete records. Differences-only uses unrounded underlying values for every primary row. An expandable semantic Details preserves exact identities, full evaluations, recipes, model revisions and all stored history as complete records. Two-identity selection and compatible dataset/objective ranking are unchanged. The parent's real stored-curve charts are retained.
- Trace renders existing activation values as a directional input/layer/output flow, with signed square tiles; all output tuple values remain present. Coordinates in diagram tiles are rounded for readability, with exact values retained in the selected-sample summary and titles. Existing provenance, sample loss and regularization stay visible.
- Backprop layer summaries present aligned large numeric columns for actual update/gradient/error metrics, actual statuses and notes. The parameter grid has an explicit higher/lower objective legend, plus unchanged real axes, min/center/max, sample basis, best direction and provenance. Request/loading/stale/retry/paused contracts are unchanged. No artificial curves or scientific values were introduced.
- Prediction's repeated description is suppressed only when identical to the already-present overlay description; specialized multiclass/regression and empty/unavailable copy remains. Checkbox labels receive 44px target height; legacy diagnostic italic styling is removed.

## Verification

- **73 focused UI tests passed across six files**: SetupEditor, InspectionPanelView, RunHistoryPanel, GuidedLessonPanel, BoundaryEvidencePanel, atelierStyles. Added a supported-neuron-limit/cancellable-draft test and a complete multiclass/signed-activation-flow test. Updated saved comparison and concise accessible-description expectations to the new intended presentation.
- Full web application and test TypeScript checks passed (`pnpm --filter @nn-playground/web typecheck`).
- ESLint passed on owned TS/TSX files; `git diff --check` passed.
- **30 actual Chromium screenshots and 38 geometry/style receipts**, using the existing dev server on 5173 and real worker outputs, are in `../../outputs/nn-forge-implementation/task-8e/`. The reproducible local script is `task-8e-browser.mjs` beside this report. Logs: `/tmp/task8e-browser.log`, `/tmp/task8e-tests.log`, `/tmp/task8e-typecheck.log`, `/tmp/task8e-lint.log`.
- Both Light and Dark: 1440×1024 network/setup/trace/gradients/saved/comparison, 1280×800 gradients, 390×844 and 360×844 prediction/setup/feature editing, 800×360 landscape Setup, and 735×860 at 200% document zoom. All captured views had no horizontal page overflow and static transport. All nine feature targets measured 44px in both mobile widths/themes. Comparison measured 2,225px full-page height, compared with approximately 5,600px in the before-review.
- At 200% zoom, focusing Model seed scrolled the field into view above the separate Apply footer. Recorded field bounds were y384–476 and footer y600–831. The 200% screenshot is a full-page record, so the footer initially appears below the initial viewport until the user scrolls; this is expected document reflow, not a floating overlay.
- Actual screenshots were inspected during iteration against the approved references, including Network setup, Trace, Gradients and mobile Setup. Intermediate captures exposed and led to fixes for CSS specificity, long coordinate overflow, footer occlusion, excessive mobile chrome, and narrow chip widths. Final mobile prediction begins around y370 with the toolbar accessible above it.

## Review boundaries

This task does not claim full 20-state visual acceptance, WebKit visual acceptance, native browser zoom/virtual-keyboard hardware testing, public deployment, or final bundle-size acceptance. Parent owns the final 40-state recapture, full suite, build/bundle budgets and paused/multiclass grid repair. Inspection composition uses only the existing display model; richer trace scatter plots or trained activation maps in a draft were not fabricated. Maximum-width architecture and comparison intentionally scroll within their own region when necessary, rather than overflowing the page.

Owned baseline files included in the commit: App; AtelierHeader/Network/Transport/ui; saved Comparison/StoredLearningChart/saved.css; SetupEditor/setup.css; RunHistoryPanel; GuidedLessonPanel; InspectionPanelView/inspection.css; BoundaryEvidencePanel; atelier.css/components.css; matching changed focused tests and the existing atelierStyles contract test. Retired stylesheet deletions, parent hooks/controller changes, engine/worker changes, docs and browser-test migrations remain with the parent.

## Follow-up: stable transport and warning-free viewport synchronization

The parent's full responsive check found an 18px mobile transport growth when the running status wrapped, plus WebKit's `ResizeObserver loop completed with undelivered notifications` during 200% Setup zoom. Fixed both causes: the status occupies exactly two lines (42px desktop, 36px mobile), and retains complete text in a keyboard-focusable scroll region for unusually long persistent reasons. Viewport observation now schedules a coalesced animation-frame update rather than writing dimensions inside ResizeObserver delivery; unchanged dimensions produce no CSS or React writes, and cleanup cancels pending work. No warning is suppressed.

Ran the three affected browser files against the existing stable localhost server, one worker, both Chromium and WebKit: `precision-lab-layout.spec.ts`, `precision-zoom.spec.ts`, `atelier-paused-visuals.spec.ts`. **26/26 passed in 1.0 minute**, comprising 22 responsive/zoom/invitation cases and four real paused-projection refresh cases. Both 390/360 transport geometry checks passed with zero Chromium layout-shift accumulation; the WebKit 768px Play/Pause flow passed; both engines passed 200% Setup/Network zoom and keyboard-focus/footer checks over viewport and zoom transitions. The tests' page-error/console assertions passed, including WebKit's prior ResizeObserver warning case. Log `/tmp/task8e-fix-browser.log`; browser artifact directory `/tmp/task8e-fix-results`. Full web application/test TypeScript checks and scoped ESLint passed (`/tmp/task8e-fix-typecheck.log`, `/tmp/task8e-fix-lint.log`).

Follow-up commit scope: AtelierTransport, only the two status-height CSS hunks, useAtelierViewport (including its previously uncommitted parent baseline), and this report. The parent's new Errors prediction-canvas/controls composition in App and its CSS, App.test assertions, screenshot-script globals comment, and browser-test migrations remain unchanged and unstaged. No build, engine/worker edit, warning filter, or subagent was used.
